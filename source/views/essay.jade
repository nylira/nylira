extends layouts/base

block title
  = meta.title

block container
  h1.title!= typogr.typogrify(meta.title)
  p.meta= 'Published on ' + meta.date + ' in the category "' + meta.category + '"'
  .body!= mdContent
